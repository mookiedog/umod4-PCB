__*/
doc/

# Ignore all the schematic and board backup files
*.s#[1-9]
*.b#[1-9]

# Ignore the files created by the CAM process
# When we generate a release tag, we will attach a zip file containing all
# the following files required to get the boards fabricated as part of the release assets.
*.GBL
*.GBO
*.GBP
*.GBS
*.GKO
*.GTL
*.GTO
*.GTP
*.GTS
*.XLN

# These files are generated by the CAM process, but are not required for fabrication:
*.gpi
*.dri

smt_files/
